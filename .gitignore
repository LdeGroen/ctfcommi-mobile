node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
*.orig.*
# Native build-output (alleen relevant bij prebuild/bare)
/ios
/android

# FCM service-account-sleutel — NOOIT committen (geheim, server-credential).
# google-services.json mag wél in de repo (client-config, nodig voor de build).
*service-account*.json
fcm-*.json
google-service-account*.json

# Android signing-keystore — NOOIT committen (signing-identiteit).
# Zit als base64 in GitHub-secret ANDROID_KEYSTORE_BASE64 + back-up in EAS.
*.jks
*.keystore
